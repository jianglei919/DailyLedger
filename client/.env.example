# 前端环境变量示例文件
# 复制此文件为 .env 并根据需要修改
# 注意：React 环境变量必须以 REACT_APP_ 开头

# =================================
# API 配置
# =================================
# 开发环境（使用 package.json 中的 proxy 配置，无需设置此变量）
# REACT_APP_API_URL=http://localhost:5000/api

# 生产环境（Render 或其他云平台部署时设置）
# REACT_APP_API_URL=https://your-backend-url.onrender.com/api

# API 超时时间（毫秒）
REACT_APP_API_TIMEOUT=10000

# =================================
# Firebase Web SDK 配置（可选）
# =================================
# 如果使用 Firebase 认证，配置以下内容
# REACT_APP_FIREBASE_API_KEY=your_firebase_web_api_key
# REACT_APP_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# REACT_APP_FIREBASE_PROJECT_ID=your_project_id
# REACT_APP_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# REACT_APP_FIREBASE_MESSAGING_SENDER_ID=000000000000
# REACT_APP_FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxxxxxxxxxxxx

# =================================
# 应用配置
# =================================
# 应用名称（可选）
# REACT_APP_NAME=Daily Ledger

# 默认语言（可选，支持 'en' 或 'zh'）
# REACT_APP_DEFAULT_LANGUAGE=en

# 调试模式（开发环境: true, 生产环境: false）
REACT_APP_DEBUG=true

# =================================
# 生产环境优化
# =================================
# 禁用 source map（生产环境推荐）
# GENERATE_SOURCEMAP=false

# =================================
# Render 部署配置
# =================================
# 在 Render Dashboard 的 Environment 标签中设置：
# REACT_APP_API_URL=https://your-api-name.onrender.com/api
# GENERATE_SOURCEMAP=false

# =================================
# 开发环境说明
# =================================
# 开发环境下，前端使用 package.json 中的 proxy 配置：
# "proxy": "http://localhost:5000"
# 
# API 请求会自动代理到后端：
# - 前端: http://localhost:3000
# - 后端: http://localhost:5000
# - API 请求: /api/... 自动转发到 http://localhost:5000/api/...
#
# 因此开发环境不需要设置 REACT_APP_API_URL

# =================================
# 生产环境说明
# =================================
# 生产环境下，前端和后端在不同的域名：
# - 前端: https://daily-ledger-client.onrender.com
# - 后端: https://daily-ledger-api.onrender.com
#
# 必须设置 REACT_APP_API_URL 指向后端完整 URL：
# REACT_APP_API_URL=https://daily-ledger-api.onrender.com/api
#
# 在 src/services/api.js 中使用：
# const baseURL = process.env.REACT_APP_API_URL || '/api';

# =================================
# 注意事项
# =================================
# ⚠️ React 环境变量必须以 REACT_APP_ 开头
# ⚠️ 修改环境变量后需要重启开发服务器（npm start）
# ⚠️ 不要将敏感信息（密钥、密码）放在前端环境变量中
# ⚠️ 前端环境变量会被打包到最终的 JavaScript 文件中，任何人都可以查看
# ⚠️ 生产环境构建时会读取 .env.production 文件（如果存在）

