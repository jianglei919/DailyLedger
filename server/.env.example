# 环境配置示例文件
# 复制此文件为 .env 并根据需要修改

# =================================
# 基础配置
# =================================
NODE_ENV=development  # development | production
PORT=5000
JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long_change_this_in_production
JWT_EXPIRE=7d

# =================================
# 数据库配置
# =================================
# ----- MongoDB 配置 -----
# 开发环境（本地）
MONGODB_URI=mongodb://localhost:27017/daily-ledger

# 生产环境（云端 MongoDB Atlas）
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/daily-ledger?retryWrites=true&w=majority

# ----- Firebase 配置（可选）-----
# 如果使用 Firebase 认证，配置以下内容
# FIREBASE_PROJECT_ID=your-project-id

# 方式 1: 使用服务账号文件（开发环境推荐）
# FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# 方式 2: 使用环境变量（生产环境推荐）
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# =================================
# CORS 配置
# =================================
# 开发环境
CORS_ORIGIN=http://localhost:3000

# 生产环境（Render 部署时使用）
# CORS_ORIGIN=https://your-app-name.onrender.com

# =================================
# Render 部署配置（生产环境）
# =================================
# 在 Render Dashboard 中设置以下环境变量：
# NODE_ENV=production
# PORT=10000
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/daily-ledger?retryWrites=true&w=majority
# JWT_SECRET=使用 Render 自动生成或手动设置 32+ 字符的强密钥
# JWT_EXPIRE=7d
# CORS_ORIGIN=https://your-frontend-url.onrender.com

# =================================
# 安全提示
# =================================
# ⚠️ 不要将 .env 文件提交到 Git
# ⚠️ JWT_SECRET 必须至少 32 个字符
# ⚠️ 生产环境使用强密码
# ⚠️ MongoDB 密码中的特殊字符需要 URL 编码
# ⚠️ 定期更换密钥和密码
# ⚠️ serviceAccountKey.json 不要提交到 Git
